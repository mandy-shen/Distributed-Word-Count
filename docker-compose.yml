version: "3"

networks:
  test:

volumes:
  tmp:

services:
  app1:
    container_name: app1
    environment:
      - id=1
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "18081:8080"
    networks:
      - test
    volumes:
      - tmp:/volume